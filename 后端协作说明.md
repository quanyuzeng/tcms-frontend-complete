# TCMS Frontend - 后端协作说明

## 项目架构概述

TCMS前端项目采用现代化的Vue 3技术栈，与后端通过RESTful API进行通信。前端负责UI展示、用户交互和国际化，后端负责业务逻辑、数据存储和权限控制。

## 前后端分离架构

### 前端职责
- ✅ 用户界面渲染
- ✅ 路由管理
- ✅ 状态管理
- ✅ 表单验证
- ✅ 国际化支持
- ✅ 数据可视化
- ✅ 响应式设计

### 后端职责
- ✅ 业务逻辑处理
- ✅ 数据持久化
- ✅ 用户认证授权
- ✅ 权限控制
- ✅ 数据验证
- ✅ 业务规则执行
- ✅ 审计日志

## 数据格式建议

### 1. 统一响应格式

#### 成功响应
```json
{
  "code": 200,
  "message": "Success",
  "data": {
    // 业务数据
  }
}
```

#### 分页响应
```json
{
  "code": 200,
  "message": "Success",
  "data": {
    "count": 100,
    "next": "http://api.example.com/users/?page=3",
    "previous": "http://api.example.com/users/?page=1",
    "results": [
      // 数据列表
    ]
  }
}
```

#### 错误响应
```json
{
  "code": 400,
  "message": "Validation error",
  "errors": {
    "username": ["This field is required"],
    "email": ["Invalid email format"]
  }
}
```

### 2. 数据类型规范

#### 用户数据
```json
{
  "id": 1,
  "username": "john_doe",
  "realName": "John Doe",
  "employeeId": "EMP001",
  "email": "john@example.com",
  "phone": "+86-138-0000-0000",
  "departmentId": 1,
  "positionId": 1,
  "role": "EMPLOYEE",
  "status": "active",
  "avatar": "https://example.com/avatar.jpg",
  "lastLogin": "2024-01-15T10:30:00Z",
  "createdAt": "2024-01-15T08:00:00Z",
  "updatedAt": "2024-01-15T10:30:00Z"
}
```

#### 课程数据
```json
{
  "id": 1,
  "code": "COURSE-001",
  "title": "Safety Training Basics",
  "description": "Basic safety training for all employees",
  "category": "Safety",
  "courseType": "online",
  "duration": 120,
  "credit": 2.0,
  "passingScore": 80,
  "instructor": "Safety Manager",
  "prerequisites": [],
  "tags": "safety,basic,required",
  "status": "published",
  "viewCount": 150,
  "enrollmentCount": 100,
  "completionCount": 85,
  "completionRate": 85.0,
  "createdAt": "2024-01-01T00:00:00Z"
}
```

## 状态管理方案

### 前端状态管理

#### Pinia Store 结构
```javascript
// 用户认证状态
const authStore = useAuthStore()
- isAuthenticated: boolean
- user: User | null
- permissions: string[]

// 应用状态
const appStore = useAppStore()
- sidebarCollapsed: boolean
- theme: 'light' | 'dark'
- language: 'en-US' | 'zh-CN'
- isLoading: boolean
```

#### 状态持久化
- 用户认证信息：localStorage
- 应用偏好设置：localStorage
- 临时状态：内存

### 后端状态管理

#### 会话管理
- 使用JWT Token进行无状态认证
- Access Token有效期：2小时
- Refresh Token有效期：7天

#### 缓存策略
- 用户权限：Redis缓存，5分钟
- 字典数据：Redis缓存，1小时
- 统计数据：Redis缓存，10分钟

## 安全建议

### 1. 认证授权

#### JWT Token结构
```javascript
// Access Token Payload
{
  "sub": "user_id",
  "username": "john_doe",
  "role": "EMPLOYEE",
  "permissions": ["course:read", "exam:write"],
  "exp": 1705320000
}
```

#### 权限控制
```javascript
// 基于角色的权限控制(RBAC)
const roles = {
  SYSTEM_ADMIN: ['*'],
  HR_MANAGER: ['user:*', 'department:*', 'training:*'],
  TRAINING_MANAGER: ['training:*', 'exam:*', 'competency:*'],
  DEPARTMENT_MANAGER: ['user:read', 'training:read', 'report:read'],
  INSTRUCTOR: ['course:*', 'exam:read'],
  EMPLOYEE: ['course:read', 'exam:take', 'profile:*']
}
```

### 2. 数据安全

#### 敏感信息处理
- 密码：使用bcrypt加密存储
- 个人信息：脱敏显示（手机号、邮箱）
- 审计日志：记录所有敏感操作

#### 输入验证
```javascript
// 前端验证示例
const rules = {
  username: [
    { required: true, message: 'Username is required' },
    { min: 3, max: 20, message: 'Length should be 3-20' }
  ],
  email: [
    { required: true, message: 'Email is required' },
    { type: 'email', message: 'Invalid email format' }
  ]
}
```

### 3. API安全

#### 请求频率限制
- 登录接口：5次/分钟/IP
- 普通接口：100次/分钟/用户
- 导出接口：10次/小时/用户

#### CORS配置
```javascript
// 允许的域名
const allowedOrigins = [
  'http://localhost:3000',
  'https://tcms.example.com'
]

// 允许的请求头
const allowedHeaders = [
  'Authorization',
  'Content-Type',
  'Accept-Language'
]
```

## 性能优化建议

### 1. 数据库优化

#### 索引设计
```sql
-- 用户表索引
CREATE INDEX idx_users_username ON users(username);
CREATE INDEX idx_users_department ON users(department_id);
CREATE INDEX idx_users_status ON users(status);

-- 课程表索引
CREATE INDEX idx_courses_status ON courses(status);
CREATE INDEX idx_courses_category ON courses(category);

-- 培训记录索引
CREATE INDEX idx_training_records_user ON training_records(user_id);
CREATE INDEX idx_training_records_plan ON training_records(plan_id);
```

#### 查询优化
- 使用分页查询，避免大数据量返回
- 使用JOIN代替子查询
- 合理使用缓存

### 2. API性能

#### 响应时间目标
- 普通查询：< 200ms
- 复杂查询：< 500ms
- 导出操作：< 5s

#### 批量操作
```javascript
// 批量删除用户
POST /api/users/batch-delete/
{
  "ids": [1, 2, 3, 4, 5]
}
```

### 3. 前端性能

#### 懒加载
- 路由懒加载
- 组件懒加载
- 图片懒加载

#### 缓存策略
- 静态资源：浏览器缓存1年
- API数据：根据业务需求缓存
- 用户权限：内存缓存5分钟

## 开发协作流程

### 1. 接口约定

#### API文档
- 使用Swagger/OpenAPI生成API文档
- 保持文档与代码同步更新
- 提供Mock数据供前端开发

#### 版本管理
```
// URL版本控制
/api/v1/users/
/api/v1/courses/

// Header版本控制
Accept: application/vnd.tcms.v1+json
```

### 2. 开发流程

#### 需求分析
1. 产品经理提出需求
2. 前后端共同评审
3. 确定接口规范
4. 并行开发

#### 开发阶段
1. 后端提供Mock API
2. 前端完成UI开发
3. 后端完成业务逻辑
4. 联调测试

#### 测试阶段
1. 单元测试（前后端各自）
2. 集成测试
3. 性能测试
4. 安全测试

### 3. 问题处理

#### Bug分类
- P0: 系统崩溃，立即修复
- P1: 功能异常，24小时内修复
- P2: 体验问题，1周内修复
- P3: 优化建议，排期处理

#### 沟通渠道
- 日常沟通：企业微信/钉钉群
- 技术讨论：技术群
- 紧急问题：电话/视频会议

## 部署建议

### 1. 环境规划

```
开发环境 (dev)
- 前端: http://dev.tcms.com:3000
- 后端: http://dev.tcms.com:8000

测试环境 (test)
- 前端: https://test.tcms.com
- 后端: https://api-test.tcms.com

生产环境 (prod)
- 前端: https://tcms.com
- 后端: https://api.tcms.com
```

### 2. CI/CD流程

```yaml
# 前端部署流程
1. 代码提交到main分支
2. 自动运行测试
3. 构建生产包
4. 上传到CDN
5. 通知部署完成

# 后端部署流程
1. 代码提交到main分支
2. 自动运行测试
3. 构建Docker镜像
4. 推送到镜像仓库
5. 更新K8s集群
6. 健康检查
7. 通知部署完成
```

### 3. 监控告警

#### 前端监控
- 页面加载时间
- API请求错误率
- 用户行为追踪

#### 后端监控
- API响应时间
- 数据库慢查询
- 服务器资源使用

## 常见问题

### Q1: 如何处理跨域问题？
A: 后端配置CORS，允许前端域名访问。开发环境使用代理。

### Q2: 如何实现实时数据更新？
A: 使用WebSocket或Server-Sent Events推送数据变化。

### Q3: 如何优化大数据量表格？
A: 使用虚拟滚动、分页加载、懒加载等技术。

### Q4: 如何处理文件上传？
A: 使用分片上传、断点续传、进度显示等功能。

### Q5: 如何保证数据一致性？
A: 前端乐观更新，后端最终一致性，提供冲突解决机制。

## 联系信息

- **前端负责人**: [待填写]
- **后端负责人**: [待填写]
- **项目经理**: [待填写]
- **技术支持群**: [待填写]