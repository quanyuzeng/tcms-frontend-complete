# TCMS Frontend - 本地部署指南

## 项目简介

TCMS (Training & Competency Management System) 是一个培训与能力管理系统的前端项目，支持中英文国际化，提供完整的培训管理、考试管理、能力评估等功能。

## 环境要求

### 必需软件
- **Node.js**: 16.0.0 或更高版本
- **npm**: 8.0.0 或更高版本（或 yarn 1.22.0+）
- **Git**: 2.20.0 或更高版本

### 推荐软件
- **VS Code**: 最新版本（推荐编辑器）
- **Chrome**: 最新版本（推荐浏览器）
- **Vue DevTools**: 浏览器扩展

## 安装步骤

### 1. 克隆项目

```bash
# 使用HTTPS
git clone https://github.com/quanyuzeng/tcms-frontend-complete.git

# 或使用SSH
git clone git@github.com:quanyuzeng/tcms-frontend-complete.git
```

### 2. 进入项目目录

```bash
cd tcms-frontend-complete
```

### 3. 安装依赖

```bash
# 使用npm
npm install

# 或使用yarn
yarn install

# 或使用pnpm
pnpm install
```

### 4. 检查安装结果

```bash
# 检查Node.js版本
node --version

# 检查npm版本
npm --version

# 检查项目依赖
npm list
```

## 项目结构

```
tcms-frontend-complete/
├── public/                 # 静态资源
│   └── logo.png
├── src/                    # 源代码
│   ├── api/               # API接口
│   ├── components/        # 公共组件
│   ├── composables/       # 组合式函数
│   ├── locales/           # 国际化翻译文件
│   ├── router/            # 路由配置
│   ├── store/             # 状态管理
│   ├── utils/             # 工具函数
│   ├── views/             # 页面组件
│   ├── App.vue            # 根组件
│   └── main.js            # 入口文件
├── index.html             # HTML模板
├── package.json           # 项目配置
├── vite.config.js         # Vite配置
└── README.md              # 项目说明
```

## 开发环境配置

### 1. 环境变量配置

创建 `.env.local` 文件：

```bash
# 复制环境变量模板
cp .env.example .env.local
```

编辑 `.env.local` 文件：

```env
# API基础URL
VITE_API_BASE_URL=http://localhost:8000/api

# 应用名称
VITE_APP_NAME=TCMS

# 默认语言
VITE_DEFAULT_LANGUAGE=en-US
```

### 2. 启动开发服务器

```bash
# 启动开发服务器
npm run dev

# 或使用yarn
yarn dev

# 或使用pnpm
pnpm dev
```

### 3. 访问应用

开发服务器启动后，打开浏览器访问：

```
http://localhost:5173
```

### 4. 登录测试

使用以下测试账号登录：

- **管理员账号**:
  - 用户名: `admin`
  - 密码: `admin123`

- **普通用户**:
  - 用户名: `user`
  - 密码: `user123`

## 构建和部署

### 1. 构建生产版本

```bash
# 构建生产版本
npm run build

# 构建结果位于 dist/ 目录
ls -la dist/
```

### 2. 预览构建结果

```bash
# 本地预览构建结果
npm run preview

# 访问 http://localhost:4173
```

### 3. 部署到服务器

#### 使用Nginx部署

1. 安装Nginx（如果尚未安装）

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx

# CentOS/RHEL
sudo yum install nginx

# macOS
brew install nginx
```

2. 复制构建文件到Nginx目录

```bash
# 创建项目目录
sudo mkdir -p /var/www/tcms

# 复制构建文件
sudo cp -r dist/* /var/www/tcms/

# 设置权限
sudo chown -R www-data:www-data /var/www/tcms
```

3. 配置Nginx

创建配置文件 `/etc/nginx/sites-available/tcms`：

```nginx
server {
    listen 80;
    server_name tcms.local;
    root /var/www/tcms;
    index index.html;

    # 启用gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API代理（可选）
    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

4. 启用站点

```bash
# 创建符号链接
sudo ln -s /etc/nginx/sites-available/tcms /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启Nginx
sudo systemctl restart nginx
```

5. 访问应用

```
http://tcms.local
```

#### 使用Docker部署

1. 创建Dockerfile

```dockerfile
# 构建阶段
FROM node:16-alpine as build-stage
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build

# 生产阶段
FROM nginx:alpine as production-stage
COPY --from=build-stage /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

2. 创建nginx.conf

```nginx
events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    keepalive_timeout 65;

    server {
        listen 80;
        server_name localhost;
        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        error_page 500 502 503 504 /50x.html;
        location = /50x.html {
            root /usr/share/nginx/html;
        }
    }
}
```

3. 构建和运行Docker容器

```bash
# 构建镜像
docker build -t tcms-frontend .

# 运行容器
docker run -d -p 80:80 --name tcms-frontend tcms-frontend

# 访问应用
# http://localhost
```

## 开发命令

### 常用命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview

# 代码格式化
npm run format

# 代码检查
npm run lint

# 修复代码问题
npm run lint -- --fix
```

### 调试命令

```bash
# 调试模式
npm run dev -- --debug

# 分析构建包
npm run build -- --analyze
```

## 常见问题

### 1. 依赖安装失败

**问题**: `npm install` 命令执行失败

**解决方案**:
```bash
# 清除npm缓存
npm cache clean --force

# 删除node_modules和package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 2. 端口被占用

**问题**: 开发服务器启动失败，提示端口被占用

**解决方案**:
```bash
# 使用不同端口
npm run dev -- --port 3000

# 或修改vite.config.js
export default defineConfig({
  server: {
    port: 3000
  }
})
```

### 3. 环境变量不生效

**问题**: 环境变量没有正确加载

**解决方案**:
- 确保环境变量文件名为 `.env.local`
- 环境变量前缀必须是 `VITE_`
- 重启开发服务器

### 4. 构建失败

**问题**: `npm run build` 命令执行失败

**解决方案**:
```bash
# 检查代码错误
npm run lint

# 清除缓存
npm run clean

# 重新构建
npm run build
```

### 5. 国际化不生效

**问题**: 语言切换没有正确工作

**解决方案**:
- 检查翻译文件路径
- 确保使用了正确的翻译key
- 检查浏览器控制台错误

## 开发工具配置

### VS Code 推荐插件

1. **Volar**: Vue 3 官方插件
2. **TypeScript Vue Plugin**: TypeScript支持
3. **ESLint**: 代码检查
4. **Prettier**: 代码格式化
5. **Auto Rename Tag**: 标签自动重命名
6. **Bracket Pair Colorizer**: 括号配对
7. **GitLens**: Git增强

### 浏览器插件

1. **Vue DevTools**: Vue调试工具
2. **React Developer Tools**: React调试工具
3. **Redux DevTools**: Redux调试工具

## 性能优化

### 1. 代码分割

```javascript
// 路由懒加载
const routes = [
  {
    path: '/dashboard',
    component: () => import('@/views/Dashboard.vue')
  }
]
```

### 2. 图片优化

```javascript
// 使用WebP格式
<img src="image.webp" alt="description">

// 响应式图片
<img 
  srcset="image-320w.jpg 320w, image-768w.jpg 768w"
  sizes="(max-width: 320px) 280px, (max-width: 768px) 700px"
  src="image.jpg"
  alt="description"
>
```

### 3. 缓存策略

```javascript
// 静态资源缓存
location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

## 国际化配置

### 添加新语言

1. 创建翻译文件

```javascript
// src/locales/es-ES.js
export default {
  common: {
    confirm: 'Confirmar',
    cancel: 'Cancelar'
  }
}
```

2. 注册新语言

```javascript
// src/main.js
import { createI18n } from 'vue-i18n'
import esES from './locales/es-ES.js'

const i18n = createI18n({
  locale: 'es-ES',
  messages: {
    'en-US': enUS,
    'zh-CN': zhCN,
    'es-ES': esES
  }
})
```

### 切换语言

```javascript
// 在组件中切换语言
import { useI18n } from 'vue-i18n'

const { locale } = useI18n()

const changeLanguage = (lang) => {
  locale.value = lang
  localStorage.setItem('language', lang)
}
```

## 联系支持

如有问题，请联系：
- 技术支持: support@tcms.com
- 项目主页: https://github.com/quanyuzeng/tcms-frontend-complete

## 许可证

本项目采用 MIT 许可证。

---

**最后更新**: 2024-01-15